<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/squit_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/squit_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity android:name=".HomeDashboardActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".PersonalizedQuitPlanActivity" />
        <activity android:name=".BreathingExerciseActivity" />
        <activity android:name=".ReadMotivationalStoryActivity" />
        <activity android:name=".ResistCravingActivity" />
        <activity android:name=".MilestonesActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>